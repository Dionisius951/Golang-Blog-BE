package routes

import (
	"github.com/gorilla/mux"
)

func userRouter(r *mux.Router) {
	// userRoute := r.PathPrefix("/users").Subrouter()

	// userRoute.HandleFunc("/login",controllers.LoginHandler()).Methods("POST")
}
